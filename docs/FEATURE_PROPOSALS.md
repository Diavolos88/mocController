# Предложения по улучшению MockController

## Текущий функционал ✅

- ✅ Прием и выдача конфигов через REST API
- ✅ Веб-интерфейс для редактирования
- ✅ Управление версиями
- ✅ Отложенное сохранение с комментариями
- ✅ Группировка заглушек по системам
- ✅ Удаление конфигов
- ✅ Хранение в PostgreSQL

---

## Предложения по новым функциям

### 🔥 Высокий приоритет (критично для работы)

#### 1. **История изменений конфигов**
**Проблема:** Нет возможности посмотреть, что и когда было изменено.

**Решение:**
- Таблица `config_history` для хранения всех версий конфигов
- В UI добавить вкладку "История" на странице конфига
- Показывать: версию, дату изменения, кто изменил (если добавить авторизацию), комментарий
- Возможность откатиться к любой предыдущей версии

**Польза:** Можно отследить, какие изменения привели к проблемам, быстро откатиться.

---

#### 2. **Массовые операции (Batch Operations)**
**Проблема:** При нагрузочном тестировании нужно менять параметры у множества заглушек одновременно.

**Решение:**
- На главной странице добавить чекбоксы для выбора заглушек
- Кнопка "Массовое изменение" открывает форму:
  - Выбор параметра (например, `loginDelayMs`)
  - Новое значение или формула (например, `+1000`, `*2`, `=5000`)
  - Применить ко всем выбранным заглушкам
- Возможность массового отката к стартовым значениям

**Польза:** Экономия времени при настройке большого количества заглушек.

---

#### 3. **Шаблоны конфигов (Presets/Templates)**
**Проблема:** Часто используются одни и те же наборы параметров для разных сценариев.

**Решение:**
- Создание шаблонов конфигов (например, "Высокая нагрузка", "Медленные ответы", "Отладка")
- Применение шаблона к одной или нескольким заглушкам
- Сохранение шаблонов в БД
- UI для управления шаблонами

**Польза:** Быстрое переключение между типовыми сценариями тестирования.

---

### 📊 Средний приоритет (улучшает удобство)

#### 4. **Экспорт/Импорт конфигов**
**Проблема:** Нужно перенести конфиги между окружениями или сделать резервную копию.

**Решение:**
- Экспорт конфига в JSON файл
- Импорт конфига из JSON файла
- Массовый экспорт/импорт (все конфиги или выбранные)
- Формат: `{systemName, startConfig, currentConfig, version}`

**Польза:** Резервное копирование, миграция между окружениями.

---

#### 5. **Поиск и фильтрация**
**Проблема:** При большом количестве заглушек сложно найти нужную.

**Решение:**
- Поиск по названию заглушки
- Фильтры:
  - По системе (auth, north, etc.)
  - По наличию запланированных обновлений
  - По версии (показать только измененные)
  - По дате последнего обновления
- Сортировка по различным полям

**Польза:** Быстрый поиск нужных заглушек.

---

#### 6. **Мониторинг и статистика**
**Проблема:** Нет понимания, какие заглушки активны, как часто обновляются конфиги.

**Решение:**
- Дашборд с метриками:
  - Количество заглушек по системам
  - График изменений конфигов за период
  - Список самых часто изменяемых заглушек
  - Статистика по запланированным обновлениям
- API для получения статистики

**Польза:** Понимание использования системы, выявление проблем.

---

#### 7. **Группы заглушек (Tags/Groups)**
**Проблема:** Нужно объединять заглушки по сценариям тестирования.

**Решение:**
- Создание групп (например, "Сценарий 1", "Критичные системы")
- Добавление заглушек в группы
- Применение операций ко всей группе
- Фильтрация по группам

**Польза:** Организация заглушек по сценариям тестирования.

---

### 🎯 Низкий приоритет (nice to have)

#### 8. **Webhook уведомления**
**Проблема:** Нужно получать уведомления о критичных изменениях.

**Решение:**
- Настройка webhook URL для каждой системы
- Отправка POST запроса при:
  - Изменении конфига
  - Применении запланированного обновления
  - Ошибке применения обновления
- Формат уведомления: `{event, systemName, timestamp, config}`

**Польза:** Интеграция с системами мониторинга, автоматизация.

---

#### 9. **API для получения истории версий**
**Проблема:** Заглушки или внешние системы хотят получить историю изменений.

**Решение:**
- `GET /api/configs/{systemName}/history` - список всех версий
- `GET /api/configs/{systemName}/history/{version}` - конкретная версия
- Параметры: `limit`, `offset` для пагинации

**Польза:** Интеграция с внешними системами, аудит.

---

#### 10. **Валидация конфигов**
**Проблема:** Можно случайно сохранить некорректный конфиг.

**Решение:**
- JSON Schema для валидации структуры конфига
- Проверка типов данных (delays должны быть числами)
- Проверка допустимых значений (loggingLv только ERROR/WARN/INFO/DEBUG)
- UI показывает ошибки валидации перед сохранением

**Польза:** Предотвращение ошибок, более стабильная работа.

---

#### 11. **Копирование конфига между заглушками**
**Проблема:** Нужно скопировать настройки с одной заглушки на другую.

**Решение:**
- Кнопка "Копировать конфиг" на странице конфига
- Выбор целевой заглушки
- Опции: скопировать текущий или стартовый конфиг
- Подтверждение перед копированием

**Польза:** Быстрое создание похожих конфигураций.

---

#### 12. **Сравнение конфигов**
**Проблема:** Нужно сравнить конфиги двух заглушек или версий.

**Решение:**
- Выбор двух конфигов для сравнения
- Визуальное отображение различий (diff)
- Подсветка измененных, добавленных, удаленных параметров

**Польза:** Анализ различий между конфигами.

---

#### 13. **Автоматическое резервное копирование**
**Проблема:** Нужна автоматическая защита от потери данных.

**Решение:**
- Настройка расписания бэкапов (ежедневно, еженедельно)
- Экспорт всех конфигов в JSON файл
- Сохранение в указанную директорию или S3
- Настройка количества хранимых бэкапов

**Польза:** Защита от потери данных.

---

#### 14. **API для массовых операций**
**Проблема:** Нужно программно управлять множеством заглушек.

**Решение:**
- `POST /api/configs/batch/update` - массовое обновление
- `POST /api/configs/batch/revert` - массовый откат
- `POST /api/configs/batch/schedule` - массовое планирование
- Формат: `{systemNames: [...], operation: "...", params: {...}}`

**Польза:** Автоматизация через скрипты, интеграция с CI/CD.

---

#### 15. **Улучшенный UI для запланированных обновлений**
**Проблема:** Сложно управлять большим количеством запланированных обновлений.

**Решение:**
- Календарь с отображением всех запланированных обновлений
- Фильтрация по системе, дате, статусу
- Массовое удаление/изменение запланированных обновлений
- Предпросмотр изменений перед применением

**Польза:** Удобное управление расписанием.

---

## Рекомендации по приоритетам

### Фаза 1 (Следующие 2-3 недели):
1. История изменений конфигов
2. Массовые операции
3. Поиск и фильтрация

### Фаза 2 (Через месяц):
4. Шаблоны конфигов
5. Экспорт/Импорт
6. Мониторинг и статистика

### Фаза 3 (По необходимости):
7. Группы заглушек
8. Webhook уведомления
9. Остальные функции

---

## Технические улучшения

### Безопасность:
- Авторизация и аутентификация (JWT, OAuth2)
- Роли пользователей (admin, viewer, editor)
- Логирование действий пользователей

### Производительность:
- Кэширование часто запрашиваемых конфигов
- Пагинация для больших списков
- Оптимизация запросов к БД

### Надежность:
- Транзакции для критичных операций
- Retry механизм для применения запланированных обновлений
- Health check эндпоинт для мониторинга

---

## Обратная связь

Если есть другие идеи или предложения - добавляйте их сюда или обсуждайте в issues.

